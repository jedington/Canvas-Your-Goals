@model MSSA.Canvas_Your_Goals.Models.Task

@{
    ViewData["Title"] = "Add Task";
}

<header> <partial name="LoggedInNav" /> </header>
<fieldset class="shadow centered">
    <legend> @ViewData["Title"] </legend>
    <form method="post">
        <input asp-for="TaskId" type="hidden" />
        <input asp-for="GoalId" type="hidden" />
        <table class="table-bordered table-striped table-sm">
            @if (ViewData.ModelState.ErrorCount > 0)
            {
                <thead>
                    <tr>
                        <td colspan="2">
                            <div asp-validation-summary="All"> </div>
                        </td>
                    </tr>
                </thead>
            }
            <tbody>
                <tr>
                    <th> Task Name: </th>
                    <td> <input asp-for="TaskName" /> </td>
                </tr>
                <tr>
                    <th> Task Order: </th>
                    <td> <input asp-for="TaskOrder" type="range" /> </td>
                </tr>
                <tr>
                    <th> Status: </th>
                    <td>
                        <select asp-for="Status">
                            <option value=""> - Select - </option>
                            <option value="Finished">Finished</option>
                            <option value="In Progress">In Progress</option>
                            <option value="Problem">Problem</option>
                            <option value="Waiting">Waiting</option>
                        </select>
                    </td>
                </tr>
                <tr>
                    <th> Start Date: </th>
                    <td> <input asp-for="StartDate" type="date" /> </td>
                </tr>
                <tr>
                    <th> End Date: </th>
                    <td> <input asp-for="EndDate" type="date" /> </td>
                </tr>
                <tr>
                    <th> Details: </th>
                    <td> @Html.EditorFor(model => model.Details) </td>
                </tr>
            </tbody>
            <tfoot>
                <tr>
                    <td class="text-center" colspan="2">
                        <input class="btn btn-primary ml-1 mr-1" type="submit" value="Create Task!" />
                        <a class="btn btn-secondary ml-1 mr-1" asp-controller="Goal" asp-action="Details" asp-route-goalId="@Model.GoalId">
                            Cancel
                        </a>
                    </td>
                </tr>
            </tfoot>
        </table>
    </form>
</fieldset>